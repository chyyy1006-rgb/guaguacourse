# V3.9.4 学业查询修正

本版针对西北工业大学翱翔教务当前移动 WebView 页面：

- 成绩页不再要求 `<tr>` / `role=row`，改为通过课程代码定位卡片式课程容器。
- 成绩页即使当前只显示课程名称与学分，也会先成功读取课程，不再整页报 DOM 未匹配。
- 如果成绩/绩点列在横向滚动区域中，会根据表头与单元格横坐标尝试自动映射。
- 手机版翱翔教务侧栏默认折叠时，会自动点击顶部“菜单”，再依次点击“成绩管理/考试管理”和“成绩信息/考试信息”。
- 自动导航最多执行 7 步，适配“打开菜单 -> 展开一级菜单 -> 点击二级菜单”的流程。
- 考试信息页面已打开但当前没有发布考试时，返回正常的 0 场结果，而不是 DOM 解析错误。

数据库版本仍为 Room V3，不需要清除应用数据。
