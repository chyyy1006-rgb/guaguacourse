# V3.9 考试安排与成绩查询

本版本在“学业”中新增：

- 考试安排：使用现有翱翔教务登录会话，只读查询考试安排；支持原始教务页面查看与重新读取。
- 成绩查询：读取课程成绩、学分、绩点、必修/其他属性，支持按学期筛选，并显示教务返回的总体 GPA / 排名（若接口提供）。
- 登录凭据不会由 App 代码读取或保存；查询在 WebView 已认证的 jwxt.nwpu.edu.cn 同源页面内完成。
- 不包含选课、退课、抢课等写操作。

数据库版本保持 V3，不需要 Migration。
