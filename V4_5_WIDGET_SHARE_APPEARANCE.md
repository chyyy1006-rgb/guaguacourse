# NPUcourse V4.5 — Widget / 导出分享 / 外观设置

本版本基于 V3.9.7 核心功能继续开发，不修改 Room 数据库版本（仍为 V3）。

## 桌面小组件

新增两种 Android 桌面小组件：

- **下一节课**：显示正在上课或下一节课、开始/结束时间、倒计时、教室。
- **今日课程**：显示当前教学周、日期，以及最多 3 条正在进行或即将开始的课程。

路径：`我的 → 桌面小组件`

课程、学期切换后 App 会主动刷新组件；系统也会按组件更新周期刷新。

## 课表图片与 ICS

路径：`我的 → 导出与分享`

支持：

- 选择第 1–20 周并生成一屏七天 PNG 课表；
- 直接分享到微信/QQ/其他支持图片分享的 App；
- 使用系统文件选择器保存 PNG；
- 将整个学期课程导出为 `.ics`；
- 分享或保存 ICS，可导入系统日历、Google Calendar、Outlook 等支持 iCalendar 的日历软件。

分享临时文件通过 `FileProvider` 暴露安全 URI，不申请存储权限。

## 外观设置

路径：`我的 → 外观设置`

新增：

- 跟随系统 / 强制浅色 / 强制深色；
- 靛紫、蓝、绿、玫瑰、橙 5 套强调色；
- Android 12+ 系统动态配色；
- 课表密度：紧凑 / 标准 / 舒适；
- 课程卡内容：简洁 / 标准 / 详细；
- 是否显示左侧节次时间。

外观选项使用 DataStore 持久化，并纳入完整备份/恢复。

## 数据兼容

- Room 数据库：仍为 **V3**；
- 无需卸载旧 App；
- 不要清除应用数据；
- 原有学期、课程、同步、提醒、备份数据保持兼容。
