# NPUcourse v4.8 产品版合并说明

本目录以用户提供的 NPUcourse v4.5 产品源码为基线，将 v4.6 ~ v4.8 测试链路中已实现的功能统一合并回正式 applicationId：`com.example.npucourse`。

## 产品版本

- applicationId: `com.example.npucourse`
- namespace: `com.example.npucourse`
- versionCode: `3`
- versionName: `4.8`
- Room database: V5（包含 V3→V4→V5 迁移）
- 完整备份 schemaVersion: 4

## 合并功能

### 1. 待办 / DDL
- 学期级、课程级待办
- 标题、备注、优先级、截止时间、完成状态
- 日期选择 + 小时/分钟滑动选择，不再手动输入时间
- 输入备注时支持 Done、空白/操作按钮收键盘，并保留强制收起键盘兜底
- 今天页显示最近待办
- DDL 可设置：不提醒、截止时、提前 10/30 分钟、1/3 小时、1/2 天、1 周
- 已删除内部测试用“提前 1 分钟”选项

### 2. DDL 通知权限与可靠性
- Android 13+ 首次进入时可引导申请通知权限
- 第一次开启 DDL 提醒时会再次检查权限
- 检查 DDL 通知渠道的横幅/铃声状态，并可直达系统渠道设置
- Android 12+ 引导精确闹钟权限；无权限时保留非精确提醒降级
- DDL 通知使用 HIGH importance、默认铃声和振动
- 通知点击直接进入“学业 → 待办”
- 完成/删除/修改待办后重新调度；开机、系统时间/时区变化后重建提醒

### 3. 课程增强
- 课程备注
- 单课程提醒开关
- 单课程提前提醒分钟覆盖
- 同步替换改为事务操作
- 学期复制/删除等多步数据库操作事务化
- 同步课程主键变化时保护关联待办，避免误删

### 4. 本周分析与空闲时间
- 当前周课程数与课内时长
- 最忙一天
- 课程时间冲突检测
- 08:00–22:00 连续空闲时间计算
- 空闲时间支持纯文字、简洁图片、卡片图片三种分享格式

### 5. Widget
- 下一节课 Widget
- 今日课程 Widget（可显示近期待办）
- DDL 倒计时 2×1 Widget
- 可缩放待办清单 Widget
- 待办 Widget 点击直达待办页
- 新增/编辑/完成/删除待办主动刷新 Widget
- 根据课程开始/结束边界安排 Widget 主动刷新，而不只依赖系统周期刷新

### 6. 数据与备份
- Room V3→V4：新增 tasks 表、课程备注和单课程提醒设置
- Room V4→V5：新增 DDL 提前提醒字段
- 完整备份升级到 schemaVersion 4
- 备份课程、学期、待办、课程备注、提醒设置、外观及桌面图标
- 保持旧 schemaVersion 1/2/3 备份恢复兼容

### 7. 外观与桌面图标
- 保留 v4.5 的主题、动态配色、强调色、密度设置
- 新增“我的 → 外观设置 → 桌面图标”
- 预置：原始绿、靛紫课表、深蓝课表、曜石课表、玫瑰课表、日落课表
- 使用 activity-alias 切换真实 Launcher 图标，无需重装
- 图标选择进入 DataStore 和完整备份

### 8. 状态与测试
- 多个子页面改为 rememberSaveable，改善 Activity 重建后的页面恢复
- 新增单双周、自定义周、冲突和空闲时间等逻辑单元测试

## 正式升级注意事项

1. 要覆盖用户现有 v4.5 安装，必须继续使用原产品签名密钥；包名相同但签名不同会被 Android 拒绝覆盖。
2. 请优先拿一台保留 v4.5 数据的设备做“直接覆盖安装”，确认 Room V3→V5 数据迁移成功。
3. 桌面图标从 MainActivity 的直接 Launcher 入口改为 activity-alias。升级后默认启用“原始绿” alias；部分 Launcher 可能需要几秒刷新图标。
4. DDL 横幅/铃声最终仍受系统通知渠道、勿扰模式及厂商省电策略控制，应用已提供权限和渠道设置引导。
