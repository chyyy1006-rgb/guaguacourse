# V3.9.1 学业查询修复

## 问题
V3.9 直接请求了翱翔教务内部接口，并猜测考试详情 URL。实际运行中出现 HTTP 500。

## 修复
- 考试安排：不再请求学生 ID，也不再拼接 exam-arrange/info/{studentId}。
- 成绩查询：不再直接 fetch getStdInfo/getMyGpa/getMyGrades。
- 两个功能都改为复用已经登录的官方 WebView：
  1. 从当前教务页面 DOM 自动寻找真实菜单入口；
  2. 点击官方菜单；
  3. 从官方页面已经渲染出的 table/DOM 中读取数据。
- 如果自动定位失败，下方会保留官方教务页面，可手动进入对应页面，再点“读取当前页面”。
- 不读取或保存密码、Cookie/token 到业务数据中。

## 测试
1. Build -> Rebuild Project。
2. 学业 -> 考试安排。
3. 若自动定位失败，手动在下方教务页面进入考试安排，点“读取当前页面”。
4. 学业 -> 成绩查询，按相同方式测试。
