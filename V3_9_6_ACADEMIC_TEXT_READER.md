# V3.9.6 学业页面文字识别

本版本停止依赖成绩内部 API，也不再把标准 table DOM 作为唯一数据来源。

核心策略：

1. 复用已登录的翱翔教务 WebView。
2. 自动扫描主页面以及所有可访问的同源 iframe。
3. 自动点击“成绩管理 → 成绩信息”或“考试管理 → 考试信息”。
4. 目标内容位于 iframe 时仍可识别，不要求顶层 URL 改变。
5. 从目标文档读取页面中实际渲染的文字及其屏幕坐标。
6. 按纵向位置重建行、按横向位置匹配“课程名称/学分/成绩/绩点”等列。
7. 如果结构化提取仍不完整，至少展示已识别的官方页面文字，不再直接报“DOM 不匹配”。

不读取密码，不把 Cookie 提取到 Android 原生层，不执行选课/退课等写操作。
