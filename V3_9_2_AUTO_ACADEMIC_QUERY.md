# V3.9.2 自动考试/成绩查询

## 主要变化

- 成绩查询自动走 `成绩管理 -> 成绩信息`。
- 考试安排自动走 `考试管理 -> 考试信息`。
- 自动点击页面中的“刷新”（如果存在），等待异步数据加载后再解析。
- 不再依赖猜测的内部 HTTP API。
- 成绩解析兼容翱翔教务当前的“固定表头 + 独立正文表”结构。
- 考试解析同样兼容表头和数据表被拆分的结构。
- 自动失败时仍保留教务原页和“读取当前页面”作为兜底。

## 安全边界

仅读取已登录 WebView 当前显示的考试/成绩信息；不读取或保存学校账号密码，不执行选课、退课等写操作。
