# V4.13.0 — 学业中心完整升级

本版本合并原计划 4.10.1、4.11.0、4.12.0 与 4.13.0 的学业功能，统一发布为 4.13.0。

## 4.10.1 查询提速与缓存

- 登录仍由翱翔门户 WebView / CAS 完成，不保存账号密码。
- 查询优先读取 WebView Cookie 后使用 Android 原生 HTTP 直连 `jwxt.nwpu.edu.cn`。
- 成绩的 GPA、学期、排名接口并发请求；各学期成绩最多 4 路并发。
- 考试优先直接下载 `/student/for-std/exam-arrange` HTML 并解析。
- 直连失败时自动回退到 4.10.0 的 WebView JavaScript bridge，保留兼容性。
- 成绩和考试查询结果持久化到本地缓存；页面打开后先显示缓存，可离线查看。
- WebView 登录完成后折叠到 1dp，避免占用主要内容区域。

## 4.11.0 图片导出与分享

- 当前学期成绩可生成专用 PNG 长图。
- 考试安排可生成专用 PNG 长图。
- 支持 Android 分享面板和保存到系统相册 `Pictures/GuaguaCourse`。
- Android 9 及以下在真正保存图片时才请求旧版存储权限。
- 默认开启导出脱敏：学号中间部分隐藏，教学班排名不写入分享图；可在页面关闭。
- 导出图包含 GPA / 学分 / 加权均分、课程成绩或考试倒计时，以及“瓜瓜课程表”来源标识。

## 4.12.0 考试与成绩提醒

- 考试默认注册考前 1 天和考前 2 小时提醒。
- 没有 Android 12+ 精确闹钟权限时自动降级为系统允许的非精确提醒。
- 成绩缓存保存时和上次快照比较，新成绩或成绩/绩点变化会触发通知。
- 默认每 4 小时低频后台检查一次；仅在已有有效教务 Cookie 时工作，登录过期后不会反复弹错误。
- 首页“今天”新增最近考试卡片，显示课程、时间、考场和倒计时，点击直达考试与成绩。
- 系统重启/修改时间后会重新建立考试提醒和后台检查。

## 4.13.0 学业分析与桌面组件

- 新增“学业分析”分页。
- 按课程学分加权计算各学期 GPA（有绩点字段时）和加权均分。
- 展示累计学分、通过学分、未通过课程数。
- 使用 Compose Canvas 绘制跨学期 GPA 趋势图，不新增图表依赖。
- 新增“学业概览”桌面组件：最近考试、倒计时、考场、当前 GPA。
- 小组件设置页由 4 种组件更新为 5 种，可直接请求添加学业概览。

## 数据与隐私

- 不新增账号/密码存储。
- 不持久化导出的教务 Cookie；直连请求只读取 WebView 当前 Cookie。
- 成绩和考试缓存仅保存在 App 私有 SharedPreferences 中。
- 后台查询依赖已有登录态，Cookie 失效后静默停止本次同步，用户重新登录即可恢复。

## 验证状态

- 新增 XML 资源均已通过 XML 解析检查。
- Kotlin 文件已做语法级编译器扫描，未发现 `expecting` / `unexpected token` 等语法解析错误。
- 当前容器无法从 `services.gradle.org` 下载项目指定的 Gradle 9.1.0，因此无法完成真实 Android Gradle 编译；最终仍需在 Android Studio/有 Gradle 9.1.0 缓存的环境执行 `./gradlew :app:assembleDebug`。
